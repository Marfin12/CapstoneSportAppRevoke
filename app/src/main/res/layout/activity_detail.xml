<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/detail_red_bg"
        tools:context=".detail.DetailActivity">

        <include
            android:id="@+id/incSportImage"
            layout="@layout/detail_card_image" />

        <include
            android:id="@+id/incSportContent"
            layout="@layout/detail_content"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="@+id/incSportImage"
            app:layout_constraintTop_toBottomOf="@+id/incSportImage" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>